# Claude Code → ACS hook mapping

Each Claude Code hook event maps to an ACS `steps/*` method. The adapter (`acs_adapter.py`) does the translation in both directions: Claude Code hook event → ACS JSON-RPC request, ACS decision → Claude Code response.

## Hook event mapping

| Claude Code hook | ACS step method | Notes |
|---|---|---|
| `SessionStart` | `steps/sessionStart` | Session bounds. Session-level — carries no `turn_id`. |
| `SessionEnd` | `steps/sessionEnd` | Session close (chain seal). Session-level — no `turn_id`. |
| `UserPromptSubmit` | `steps/turnStart` **then** `steps/userMessage` | A prompt OPENS a turn: the adapter first emits a decision-eligible `steps/turnStart` (`triggered_by: user_message`; a Guardian MAY `deny` to block the whole turn), then the `steps/userMessage` carrying the `prompt`. Both — and every in-turn step below — carry `metadata.turn_id`. The `turn_id` is held in per-session state until the matching `Stop`. |
| `Stop` | `steps/turnEnd` | End of a **turn** (every reply), not the session. `steps/sessionEnd` "seals the chain" once per session, so mapping `Stop` there sealed it after every reply; `steps/turnEnd` (audit-only, `outcome: completed`) is the correct per-turn boundary and echoes the open turn's `turn_id`. A `Stop` with no open turn is skipped + audited (`turn_end_no_open_turn`) rather than emitting a fabricated `turn_id`. |
| `PreToolUse` | `steps/toolCallRequest` | Fires before the tool runs. Decision gates execution. Carries `metadata.turn_id`. |
| `PreToolUse` with `tool_name="Agent"` (legacy: `"Task"`) | `steps/subagentStart` | The `Agent` tool IS Claude Code's subagent spawn (per the hooks reference; older builds named it `Task`, still matched), so it routes to the confused-deputy gate (proposed Core floor in PR #21, which is not part of this branch) instead of the generic tool hook. All four required fields are real: `parent_step_id` is this step's own deterministic request_id (the delegation step and the spawn event are the same wire step on this platform), `subagent_session_id` is a uuid5 of the `tool_use_id` under a distinct namespace, `intent_derivation` is `derived_from_parent` (the Agent prompt is parent-authored — never `"fresh"`). The Guardian's decision translates back through the normal PreToolUse `permissionDecision` shape. `PostToolUse` for the Agent call stays `steps/toolCallResult` (it is honestly the tool's result); the separate `SubagentStop` event is not forwarded (see its row below). |
| `PostToolUse` | `steps/toolCallResult` | Fires after the tool runs. `tool_response` carries the output. |
| `SubagentStop` | — not forwarded | Deliberately unmapped (`KNOWN_UNMAPPED`): `steps/subagentStop` REQUIRES `final_chain_hash` in the current schema, and Claude Code maintains no session chain — the value is genuinely unknowable, and a fabricated hash would corrupt the integrity artifact the field exists to produce. A separate schema PR proposes making the field optional for chain-less frameworks; honest wiring resumes when the spec owners approve it (same stance as the Cursor adapter). |
| `Notification` | `steps/agentResponse` | **Observation-only.** Claude Code's `Notification` fires *after* the assistant message is delivered to the user; the framework does not consult the hook return value. The adapter emits the ACS envelope for trace + audit, but a Guardian `deny` / `modify` cannot retroactively block or rewrite a message the user has already seen. ACS-Core §hooks.md describes `agentResponse` as decision-eligible; this adapter's mapping is honest about the framework constraint. |

**Deliberately not mapped — `PreCompact`.** `steps/preCompact` requires `entries_to_compact` (`minItems: 1`) — the step_ids being folded into the summary, which is what the post-compaction provenance lineage references. Claude Code's `PreCompact` event carries no entry list, so an honest payload cannot be built; an empty or fabricated list would defeat the hook's provenance-laundering defense. This is a documented Guardian visibility gap (the adapter's `KNOWN_UNMAPPED` set), not an oversight.

**Deliberately not mapped — the skill lifecycle set (`steps/skillRegister`, `steps/skillLoad`, `steps/skillUnload`).** Claude Code fires no hook event when a skill registers, activates, or unloads, and the payloads' integrity fields (`definition.digest`, the registration-bound load path) appear in no hook event, so an honest payload cannot be built. A skill *invocation* is governed: it arrives as `PreToolUse` for the `Skill` tool and routes through `steps/toolCallRequest` like any other tool call. Lifecycle wiring becomes possible when the platform exposes skill lifecycle events.

Any other Claude Code hook event reaching the adapter (upstream renames, events wired in `settings.json` that the adapter doesn't know) emits an `unmapped_hook_event` audit line and lets Claude Code proceed — an ungoverned event is recorded as ungoverned, never silently dropped. Unmapped today: `PostToolUseFailure`, `PermissionRequest`, and other lifecycle events with no semantic ACS equivalent in v0.1.0.

## Disposition mapping

Block-shape hooks are only those where a block actually PREVENTS the action: `PostToolUse` and `UserPromptSubmit`. `Stop` is **audit-only** — Claude Code reads `{"decision":"block"}` on it as "do NOT stop, keep going," so the adapter never emits a block there; a Guardian `deny` is recorded (`unenforceable_decision`) and nothing is emitted. (`SubagentStop` is not forwarded at all — see the mapping table.)

| ACS disposition | Claude Code response on `PreToolUse` | Response on `PostToolUse` / `UserPromptSubmit` |
|---|---|---|
| `allow` | `{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "allow"}}` | empty (Claude Code proceeds) |
| `deny` | `…"permissionDecision": "deny", "permissionDecisionReason": "…"` | `{"decision": "block", "reason": "..."}` |
| `modify` | `…"permissionDecision": "allow", "updatedInput": {…}` — the `parameter_overrides` are MERGED onto the original input (Claude Code replaces the whole input, so sending overrides alone would drop other arguments); substituted to `deny` if there is no usable object | `PostToolUse`: `updatedToolOutput` when `modified_content` present, substituted to `block` otherwise |
| `ask` | `…"permissionDecision": "ask"` — **native**; Claude Code surfaces its own approval prompt | substituted to `block` |
| `defer` | substituted to `deny` + `defer_substituted_deny` audit event (no native defer in `permissionDecision`) | substituted to `block` |

An arrived decision the host CANNOT apply is substituted to a block/deny (never silently proceeded) and audited. Note the substitutions here are **deployment behavior, not spec-mandated**: §9.2 places disposition substitution on the Guardian, and §6's `timeout_decision: deny` default governs an *expired* defer, not an instant client-side substitution. A deployment that needs approver-driven resumption should run ACS-Core's ASK/DEFER flow at the Guardian layer and have the Guardian return an effective decision before responding to the adapter. A contradictory `modifications` object (both wholesale + structured, or overlapping targets) fails closed to `deny` per §6.3:146.

## Conformance posture

The Claude Code adapter implements ACS-Core's mandatory floor. Every row below describes what the code in `acs_adapter.py` actually does — this table drifted from the code once (PR #22 review caught six false claims in it), so treat the code as ground truth and this table as its description:

- Handshake: `handshake/hello` is performed once per session (idempotent via disk cache, `~/.cache/acs-adapter-handshake/`) before the first hook event. The ServerHello's response envelope is signature-verified and bound to the ClientHello before caching; its `on_decision_failure` participates in the effective fail posture (most-restrictive-wins with `ACS_DEFAULT_DENY`). Handshake failures are negative-cached for a short TTL so a dead Guardian costs one timeout, not one per event. `ACS_HANDSHAKE=0` disables.
- Turns: a `UserPromptSubmit` opens a turn with an explicit `steps/turnStart` (decision-eligible) before the `steps/userMessage`; `Stop` closes it with `steps/turnEnd`. The `turn_id` is held in per-session state and stamped on `metadata.turn_id` of every in-turn step; session-level steps (`sessionStart`/`sessionEnd`) carry none.
- Dispositions: ALLOW / DENY / ASK are honored natively on `PreToolUse` (`permissionDecision`); MODIFY via `updatedInput` (overrides merged onto the original input) when a usable `parameter_overrides` object is present; DEFER substitutes to deny with an audit event. **Post-execution and lifecycle hooks (`PostToolUse`, `Notification → agentResponse`, `Stop → turnEnd`, `SessionEnd`, `SubagentStop`) are observation-only**: Claude Code fires them after the action / message / turn / session has completed; a Guardian `deny` cannot undo the side effect. Deployments needing pre-delivery gating must place the gate at `UserPromptSubmit` (prompt / turn start) for prompts or `PreToolUse` for tools.
- Session context: `session_id` comes from Claude Code's own hook event field on every request; the Guardian-side audit chain accumulates against it. `agent_id` is `ACS_AGENT_ID` when set, else derived as `claude-code:<sha8(cwd)>`.
- Replay protection: `request_id` (UUID) and `timestamp` are populated on every request. `PreToolUse` request_ids are deterministic (UUID5 of Claude Code's `tool_use_id`) so the matching `PostToolUse` can cite `request_id_ref`.
- Baseline integrity: HMAC-SHA256 per §10 over the RFC 8785 (JCS) canonical envelope, with an HKDF-derived per-session key from `ACS_HMAC_SECRET_FILE` / `ACS_HMAC_SECRET`. `rfc8785` is a hard dependency (no fallback canonicalization — §10 permits none). Responses are signature-verified AND bound to their request (`id` + `request_id`); a mis-bound or badly-signed response fails closed regardless of posture. Without a secret the adapter runs unsigned and says so with a loud `unsigned_mode` audit event.
- Decision honoring: the fail posture defaults to `proceed` (fail-open with audit event), matching §6.4's default. `ACS_DEFAULT_DENY=1` or a ServerHello `on_decision_failure: deny` flips it to fail-closed (most-restrictive-wins). Guardian **refusals** (`SIGNATURE_INVALID`, `REPLAY_DETECTED`, `TIMESTAMP_OUT_OF_WINDOW`, malformed/oversized envelope) always fail closed regardless of posture — a refusal is an alive Guardian rejecting the envelope, and each refusal is attacker-reachable (spec issue #32).
- Audit: every fail-open proceed, refusal, substitution, and unmapped event emits an `ACS_AUDIT` line to stderr and, when `ACS_AUDIT_FILE` is set, appends to that file (0600).
