# Conformance

This table compares the Go Guardian with ACS-Core as defined on `integration` in
[docs/spec/conformance.md](../../../docs/spec/conformance.md). Rows affected by
[the open PR #21](https://github.com/GenAI-Security-Project/agent-control-standard/pull/21)
say so explicitly.

ACS v0.1.0 has no external certification mechanism. The Go Guardian self-declares
`acs-core`; each claimed row names the tests that cover it. `make docs-check`
verifies that those tests and the document's local links exist. It cannot prove
that a test's assertions establish a claim; code review and `make check` provide
that assurance.

| ACS-Core item | Status | Tests | Note |
| --- | --- | --- | --- |
| Handshake, `handshake/hello` (§4) | Met | `guardian.TestHandshake`<br>`guardian.TestRecoveredSessionUsesNegotiatedTerms`<br>`guardian.TestHandshakeRefusals`<br>`guardian.TestHandshakeReplay`<br>`guardian.TestHandshakeReplayAfterSessionEnd`<br>`guardian.TestHandshakeAtCapacity`<br>`guardian.TestKeyBoundToSession`<br>`guardian.TestAgentIDBoundToSession`<br>`internal/handshake.TestSelectVersion`<br>`internal/handshake.TestEvaluatedMethods`<br>`internal/handshake.TestPartialObservedAgentIsNegotiated`<br>`internal/observedagent.TestHandshakeIgnoresUnknownResultFields`<br>`internal/hostadapter.TestHandshakeErrorIsRefusal`<br>`internal/hostadapter.TestLoadStateDefaultsAnOmittedFailurePosture` | Negotiates the version, the methods offered by the Observed Agent, the transport, the timeouts, the skew window and the failure posture; refuses with -32001, -32002 and -32000. `profiles_accepted` is the intersection this Guardian accepts from `profiles_supported`; a partial Observed Agent may honestly declare only the hooks it emits. The signed handshake binds the session to its `key_id` and `agent_id`; later steps and a restart handshake must carry both values. A second handshake for a negotiated session is refused, because v0.1 defines no renegotiation, unless it carries the same ClientHello under those terms: then it returns the saved ServerHello and changes nothing. Every replica uses those saved terms. A replayed handshake is -32005, before or after session end. A full store refuses a new session rather than drop a live one. |
| Request and response envelopes, JSON-RPC 2.0 (§3) | Met | `guardian.TestEnvelopeGate`<br>`guardian.TestTransportErrors`<br>`guardian.TestMalformedSignatureIsNotSent`<br>`internal/schema.TestSchemasMatchSpecification`<br>`internal/schema.TestWireTypesMatchSchemas`<br>`internal/observedagent.TestSendRejectsAResponseForAnotherRequest`<br>`internal/observedagent.TestSendIgnoresUnknownResultFields`<br>`internal/observedagent.TestSendRejectsMalformedHandshakeResult`<br>`guardian.TestBodyCap` | Every request is validated against `request-envelope.json` (-32600), and a request for a method the Guardian handles also against its payload schema (-32602, §17). Every result is validated against `response-envelope.json` as the bytes to be sent, signature included. The bundled Observed Agent validates each answer before honoring it, ignores unknown result fields and binds it to the request method and identifiers. Batches are answered -32600, as §3 permits. |
| Hook taxonomy: sessionStart, userMessage or agentTrigger, toolCallRequest, toolCallResult, agentResponse, sessionEnd (conformance.md), and subagentStart (PR #21) | Met; ACS leaves part undefined | `guardian.TestCoreFloor`<br>`guardian.TestSkillLoadNeedsApprovedRegistration`<br>`guardian.TestPostCompactLineage`<br>`guardian.TestPostCompactLineageSurvivesEntryArchival`<br>`guardian.TestTurnIDReachesTheChain`<br>`internal/method.TestRoute` | All nineteen native hooks are dispatched and decided; each hook's permitted dispositions follow hooks.md. The Guardian applies the rules hooks give it directly: a skillLoad whose (`skill_id`, digest) no allowed skillRegister approved is DENY `skill_unverifiable`; a postCompact whose `pre_compact_chain_hash` is not the head it was appended to, or whose summary's `derived_from` is not the union of the compacted entries' `provenance_id`s, or whose origin is not `agent_generated`, is recorded with `lineage_mismatch` (the hook permits no DENY); archived entries keep their lineage; `metadata.turn_id` is written onto each entry that carries it. ACS does not define a `turn_id` member on `context-entry.json`, so the Go Guardian uses the envelope field's name. ACS also asks the sender for a `post_compact_chain_hash` that the sender cannot know; the Guardian publishes the actual hash in its answer instead. Trust monotonicity over a summary's lineage is policy, and is the engine's. |
| Dispositions: all five with their required fields (§6) | Met | `guardian.TestCoreFloor`<br>`guardian.TestEngineFailuresAreDenials`<br>`guardian.TestHookPermittedDispositions`<br>`internal/disposition.TestCheck` | A decision missing a required field is answered DENY. The disposition table below says which dispositions AGT itself emits. |
| MODIFY composition (§6.3) | Met | `internal/disposition.TestCheck`<br>`internal/observedagent.TestSendFailsClosedForMixedModifyShapes`<br>`internal/hostadapter.TestModifiedToolCallInput`<br>`internal/hostadapter.TestModifiedToolCallResult`<br>`guardian.TestEngineFailuresAreDenials`<br>`guardian.TestWrappedMCPRefusesOverlappingModifications` | A modification that combines a replacement with edits, or edits overlapping fields, is never sent. Host adapters apply only changed argument or result values and refuse changes to fields their host cannot apply. Wrapped MCP overrides are checked against their effective paths under `params.arguments`. |
| MODIFY for an Observed Agent that cannot apply it (PR #21, proposed §6.5) | Met | `guardian.TestClientSubstitutions` | Not in v0.1.0 on integration; implemented as PR #21 proposes it, and reached only when the engine reports an Observed Agent that cannot apply MODIFY. DENY with `modify_unsupported`, or ALLOW at postCompact, with an audit event. |
| ASK for an Observed Agent that cannot resolve it (§9.2) | Met | `guardian.TestConfiguredAskSubstitution`<br>`guardian.TestClientSubstitutions` | `Config.AskSubstitution` selects the endpoint-wide substitute. An engine can report a more specific trusted signal in `PolicyDecision.Client`. Both forms return DEFER with `timeout_decision: deny`, or DENY with `approver_unavailable`, with an audit event. |
| ASK approver and intent extension (§9, §9.1) | Met; ACS leaves part undefined | `guardian.TestIntentExtension`<br>`guardian.TestEngineFailuresAreDenials` | An ASK names an approver type declared in `ServerHello`. A grant must answer a step that received ASK in the same session; otherwise the Guardian returns DENY `grant_invalid`. A session-scoped grant extends `Intent.parsed` and creates an `intent_extension` ContextEntry. That entry commits the approver, capabilities and extension provenance to the chain. The engine authenticates the approver and applies the session's `scope_mode` before returning the grant. ACS v0.1.0 defines no wire method for the approver's answer (#175) and no ContextEntry fields for the grant. The Go Guardian uses `approver` and `intent_extension`, the names from `ask-details.json`. |
| DEFER details and the cascading-deferral bound (§6) | Met; ACS leaves part undefined | `guardian.TestCoreFloor`<br>`guardian.TestDeferralBound` | `timeout_decision` defaults to deny; a session's deferrals past the configured bound are answered DENY. ACS v0.1.0 defines no way for a resolved DEFER to reach the Observed Agent. The Go Guardian adds no wire method for it. The engine can decide the step when the Observed Agent submits it again. |
| SessionContext and published chain head (§8.2, §8.6) | Met | `guardian.TestChainPublication`<br>`internal/chain.TestVector`<br>`internal/chain.TestEntryHashCommitsToEveryMember`<br>`guardian.TestChainMismatch`<br>`guardian.TestReserveAndAppendAtomicity` | Every content-bearing step is appended before it is decided, `request_hash` is always written, and `chain_hash` is on every answer and covered by its signature. An Observed Agent that sends a different `chain_hash` receives DENY with `chain_mismatch`. A store orders each session across every Guardian that shares it. A store with an expiring claim cancels `lockCtx` when ownership is lost and fences every mutation against the claim. Each append also checks the expected head. |
| Intent immutability (§8.4) | Met | `guardian.TestIntent` | Intent is established at sessionStart or the first agentTrigger; a step carrying a different Intent is answered DENY with an audit event. |
| Replay protection (§10.3) | Met | `guardian.TestReplayAndSkew`<br>`guardian.TestRecoveredSessionUsesNegotiatedTerms`<br>`guardian.TestHandshakeReplay`<br>`guardian.TestHandshakeReplayAfterSessionEnd`<br>`guardian.TestCoreFloor`<br>`guardian.TestWithinSkew`<br>`guardian.TestReplayHistoryOutlivesEveryAcceptedTimestamp`<br>`guardian.TestReplayReservationsAreBounded` | A repeated `request_id` or nonce in a session is -32005, including a replayed handshake and a step after sessionEnd. A timestamp outside the session's saved `skew_window_ms` is -32006 with the window in `data`. The default store keeps a session for at least two skew windows after its last write, so an accepted future-dated request is stale before its replay history can be removed. Replay reservations are bounded independently of accepted entries. |
| Baseline integrity: HMAC-SHA256 over the §10 canonical input (§10) | Met; ACS leaves part undefined | `guardian.TestSignatureRequired`<br>`guardian.TestReplayAndSkew`<br>`guardian.TestHMACSignerVectors`<br>`guardian.TestIndependentVerifier`<br>`guardian.TestKeyBoundToSession`<br>`guardian.TestAgentIDBoundToSession`<br>`internal/envelope.TestSigningInputVectors` | Every request but system/ping must be signed. An answer to a request that authenticated is signed at `result.signature` or `error.signature` while the signer is available; a ping's answer is signed only when the ping was signed. A request that did not authenticate has established no key, so its answer is unsigned. A signer failure can only produce an unsigned internal error. ACS does not define where a ServerHello or error signature is stored. ACS also requires HKDF without defining all of its inputs. The exact behavior implemented here is listed below. |
| Decision honoring and `on_decision_failure` (§6.4) | Observed Agent responsibility | `guardian.TestHandshake`<br>`guardian.TestRecoveredSessionUsesNegotiatedTerms`<br>`guardian.TestStoreFailureIsSignedDenial`<br>`guardian.TestAuthenticatedStepProviderPanicIsSignedDeny`<br>`guardian.TestKeyBoundToSession`<br>`guardian.TestAgentIDBoundToSession`<br>`guardian.TestEngineCallsAreBounded`<br>`guardian.TestPolicyStateIsBounded`<br>`guardian.TestPolicyStateMustBeJSON`<br>`guardian.TestShutdownBoundsAnEngineThatIgnoresCancellation`<br>`cmd/guardian.TestShutdownDeliversCancellationDenial`<br>`guardian.TestTamperedDecisionFallsToThePosture`<br>`internal/observedagent.TestHonour`<br>`internal/observedagent.TestSendIgnoresUnknownResultFields`<br>`acs-codex-hook.TestRunRecordsAProceedFailure`<br>`acs-opencode-hook.TestRunGovernsRequestAndResult`<br>`acs-opencode-hook.TestRunBlocksEveryNonProceedingDisposition` | The Observed Agent must honor the decision or apply the negotiated failure posture when no usable decision arrives. The test Observed Agent covers that behavior. Each session keeps its negotiated timeout. The Guardian returns a signed decision for every authenticated step while signing remains available, including an engine failure, store failure, provider panic, identity mismatch, capacity refusal or shutdown. Process shutdown gives cancellation answers their own bounded delivery phase. The [OpenCode plugin callback test](../examples/opencode/plugin.test.mjs) verifies that the real plugin applies decisions to completed and failed tool results. |
| Liveness, `system/ping` (§13; SHOULD under PR #21) | Met | `guardian.TestPing` | A valid ping is always ALLOW, needs no signature and writes nothing to the SessionContext. A malformed one gets a JSON-RPC error, never an ACS-specific one. |
| Wrapped MCP, `protocols/MCP/*` (conformance.md; only for sessions involving MCP under PR #21) | Met | `guardian.TestWrappedMCP`<br>`guardian.TestWrappedProtocolRouting`<br>`guardian.TestWrappedMCPRefusesOverlappingModifications`<br>`internal/handshake.TestEvaluatedMethods`<br>`internal/mcp.TestCheck`<br>`internal/mcp.TestSupports`<br>`agtbridge.TestWrappedMCPToolCalls`<br>`agtbridge.TestProjectionReaddress`<br>`agtbridge.TestProjectionKeepsEveryMCPResultShape`<br>`agtbridge.TestProjectionRemembersOnlyWhatRuns`<br>`agtbridge.TestProjectionRefusesAPendingID`<br>`agtbridge.TestProjectionCanonicalizesMCPIDs` | `protocols/MCP/*` and the explicit-version `wrapped:mcp-<version>/*` both route to the MCP handler, which reads the wrapped JSON-RPC 2.0 message, a request or the MCP response the Observed Agent received, and refuses a malformed one with -32602. An explicit-version method is negotiated only at the version declared in `wrapped_protocols`. The message reaches the engine intact. AGT decides a wrapped `tools/call` with the same tool gates as a direct tool call. The projection preserves text, image, audio, resource, structured content, metadata and error data. A redaction is addressed back into the MCP message. A parameter override names a member of `params.arguments`. A second call under an equivalent MCP id still awaiting its response is denied, so each result meets its own tool's rules. |
| Policy engine interface, `delegate_to` (§12.1) | Met | `guardian.TestEngineFailuresAreDenials` | `delegate_to: agent` is answered DENY with `agent_layer_unavailable`: this Guardian has no agent layer (§12.2 is optional). |

## Dispositions

The Guardian supports all five, each covered with a test engine. The default
AGT policy bridge uses the checked-in policy deployment and emits fewer;
running it does not exercise all five.

| Disposition | Guardian | Checked-in AGT policies |
| --- | --- | --- |
| allow | yes | yes: `allow`, and `warn` with a policy reference |
| deny | yes | yes: `deny`, and every `runtime_error:*` |
| modify | yes | yes: `transform`, the shipped redaction rules |
| ask | yes | only with an approver configured; the checked-in configuration never escalates, and without an approver an escalate is DENY `approver_unavailable` |
| defer | yes | no: mapping.yaml maps no AGT verdict to defer |

## Behavior that ACS v0.1.0 leaves undefined

ACS v0.1.0 requires each result listed below, but it does not fully define the
wire field or algorithm needed to produce that result. The second column states
the exact behavior implemented here. Two implementations must make the same
choice to interoperate on that point.

| What ACS v0.1.0 leaves undefined | Behavior implemented here |
| --- | --- |
| ACS-Core requires every answer to be signed, but `response-envelope.json` defines no signature field for an error. | An error answering an authenticated request stores the signature in `error.signature`. This matches the `signature` field of an `AcsResult`, and the error schema permits the additional field. The Python adapters in PR #22 use the same location. An error answering an unauthenticated request is unsigned. |
| §10 requires every response to be signed, but `handshake.json` defines no signature field for `ServerHello`. | `ServerHello` stores the signature in `result.signature`. This matches the `signature` field used by an `AcsResult`, and the `ServerHello` schema permits the additional field. |
| §10 requires HKDF for the per-session key, but does not define the hash function, salt, `info` input or output length. | The signer derives a 32-byte key with HKDF-SHA256. It supplies no salt and uses the UTF-8 bytes of `session_id` as the HKDF `info` input. `salt` and `info` are inputs to HKDF; they are not fields in an ACS message. |
| turnStart requires `turn_id` on every per-step ContextEntry; `context-entry.json` names no member for it. | The entry carries `turn_id`, the envelope field's name, inside its hash. |
| §9.1 requires an `intent_extension` entry to record the approver, the capabilities and the extension's provenance; `context-entry.json` names no members for them. | The entry carries `approver` and `intent_extension`, the names `ask-details.json` uses, inside its hash. |
| ACS v0.1.0 defines no wire method for sending an approver's answer to the Guardian (#175). | The `PolicyEngine` receives the answer by its own means. On a later policy evaluation, the engine authenticates the approver and returns the grant. The Guardian then checks that the grant answers an ASK from the same session. |
| `post-compact.json` asks for `post_compact_chain_hash`, the chain head after the entry that carries it; that entry's `request_hash` commits to the value, so no sender can compute it. | The value is not checked; the answer's `chain_hash` publishes the head. `pre_compact_chain_hash` is checked. |
| §6.3's `parameter_overrides` name tool arguments; nothing says what they name in a wrapped MCP message. | For a wrapped `tools/call`, they name members of `params.arguments`. |
| ACS v0.1.0 defines no way to notify the Observed Agent that a DEFER has been resolved. | The Go Guardian adds no wire method. The Observed Agent may learn of it by other means, outside ACS. The Observed Agent can then submit the step again, and a policy engine that remembers the resolved case can return the final decision. |
